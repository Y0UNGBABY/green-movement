# Green Movement agent guide

This project turns a GitHub contribution grid into an animated miniature pasture SVG.

- Resume from `docs/nulnul/evolution.json`; do not reconstruct the plan from chat.
- Judge visual work against `docs/nulnul/visual-benchmark.md` at 700px on GitHub's dark background.
- Use the preserved green-movement motion study and measured frames as the design authority. Do not activate `frontend-design`; research again only for a named gap the project study does not cover.
- Run `npm run check` after source changes. Run `npm run visual:fixture` when motion or rendering changes.
- The frozen fixture baseline is 4,099,243 bytes; growth above 15% needs a measured visual benefit.
- Keep the scene disciplined: derive the simultaneous field budget from total contribution energy as 0/1/2/4/6 sheep, reducing only when distinct reachable food/drop assignments are impossible. Keep the original UFO glyph, no flowers, and no ambient UFO wobble or banking.
- Flower rendering has been removed; keep only the negative fixture assertion that prevents it from returning.
- Keep `assets/sheep.svg` and `assets/ufo.svg` visually synchronized with their inline constants.
- Current motion guardrails: 0/1/2/4/6 planned field sheep; one opening inventory batch lasting `0.48 + 0.30 × fieldCount` source seconds before the 0.14-source-second / 0.182-playback-second first entry; the same 0.14 rhythm across later deployment, collection, finale convergence, and exit; one 0.86-source-second / 1.118-playback-second body-hidden turnover service interval containing a 0.76-source-second / 0.988-playback-second panel exchange; at most 8px of visible field motion at each edge; and one existing green light plus one travel streak through the hidden middle. Keep field UFO-body opacity snap-only at 0 or 1 and paint the opaque stopped body above field sheep. Deduplicate reachable food candidates, prefer restrained three-to-four-edge real routes between bites, and let a full outgoing sheep follow its recorded cleared approach through the final pre-work cell before pickup. Keep one distinct field SVG per identity, no offscreen sheep transform, exact-cell materialization, no stale outgoing coordinate, no fake idle motion, no movement before the departing UFO body disappears, and direct first-work-cell replacement drops with no artificial bridge.
- Current inventory guardrails: exactly eight fixed pens containing only the next undispatched sheep, with `양떼 current/total` placed to their left and the rightmost pen doubled as the unboxed transfer bay. Before the first flight, open that pen's top gate once and board exactly the 1/2/4/6 opening sheep in sequence. Move the dock UFO down 18.5px to the exact centre of the rightmost pen while the sheep stays fixed in the slot and disappears only under the foreground craft and absorption opacity; use no boarding clip or vertical sheep transform. Preserve all eight slot coordinates through every opening load so vacancies accumulate without an old sheep jumping left. Hold absorb and empty beats before each adjacent right shift, leave the bay visibly empty while the UFO lifts after the final opening load, and keep those vacancies until the final deployment UFO is fully offstage. Then shift the survivors once and reveal missing waiting sheep individually from the far-left pen toward the right; whenever at least eight sheep remain undispatched, the stable queue must use all eight pens. Later dispatches must follow approach→settle→absorb→lift→shift→refill→settle→drop: the hidden next sheep may appear at far left only after the seven old occupants finish shifting and the dock UFO is clear. Keep the dock core steady rather than flashing per sheep, all source slots within 0–7, count total→0 monotonically, and never animate a sheep from outside the pen grid or replay/requeue a collected sheep.
- Current sheep guardrails: relative per-user active-week appetite in deterministic 6 high / 1 normal / 3 low groups with capacities averaging 20; identical initial size; bite-driven growth with a late-readable curve and an approximately 1.20 high/low mature-size ratio; one varied-color 1×1 ear tag with theme-safe outline; and no new cargo or energy actor. Stored energy may brighten only the existing pickup light, the fixed dock UFO core while it absorbs, and the existing final cell wave.
- Current panel guardrails: the pasture-matched 53×5 grid, 12px inset/pitch, borderless 18/17/18-column status, fixed x=216–636 36×4 bite map, one latest-bite cursor, exact `포만 n/capacity` plus ten separated cells, and one 190×40 1.30x exact-field camera. Choose a bounded deterministic mix of long-lived context, route, and grazing heroes; hold each through collection; keep reframes chronological and outside visible inventory motion; blank camera, HUD, and footprints for the 0.55-source-second hero handoff; and use snap-only selected visibility. At 350px strengthen existing text and strokes and suppress only microscopic secondary motion. Never restore a full roster, roster number, space-between layout, replacement cards, V-shaped checks, `ufo-hover`, `ufo-bank`, blur, or filter. Keep one deterministic 1.3x pace; runtime may exceed the former 50-second target only for a measured continuity benefit.
- Stage UFO movement as turn, travel, stop: face the next destination during the final 100ms before departure, use the shortest rotation, and hold the exact position until each actual departure.
- Limit cinematic support to one travel-only streak; hide it and the nonessential ripple and crumb motion for `prefers-reduced-motion`.
- Keep the UFO offstage between deployment and pickup so the flock owns the middle act. For every service leg, show only a short departure, hide the UFO body while the existing green light carries the fast middle, show a short approach, then stop exactly at the destination.
- Sheep headings must take the shortest turn; animate the body only while walking and use one head nod per multi-tick stop instead of a stationary tick pulse.
- Drive grass, crumbs, and grazing poses from the sheep's deployment-delayed visual arrival time, never the earlier simulation clock.
- Drive bite acting only from recorded grass arrivals, never from a generic stationary frame. Stage every bite inside one 240ms hold: brace from 60ms before arrival, retract at arrival, synchronize full head extension, body compression, first grass step, and crumb release at 120ms, restore by the 230ms final grass step, then depart at 240ms. A collision wait must remain neutral; do not add shake or flash.
- For the first entry, keep the body hidden until the final 8px approach to the first planned drop; every later leg uses the complete shared rhythm. Preserve the left-to-right zigzag order and exact stops. Do not show a clipped offstage body, cross-field body, horizontal double-back, scan line, crosshair, drop marker, or other HUD.
- Each sheep remains fixed through its landing reveal and UFO release hold, then starts moving only after the departing UFO body is hidden. Do not reintroduce relay breaths or pair handoff waits. The representative fixture clears all 201 grass cells before pickup.
- Give the middle act causal turnover: keep each camera hero through its full visible field lifetime, change heroes only after collection, pan the live crop for exactly 0.55 source seconds with its HUD and footprints blank, then reveal the next hero and matching fullness together at camera arrival. Field replacements remain distinct clean white actors revealed only at their exact drops and held under the fully opaque stopped UFO until its body disappears before starting their bridges. At pickup, keep the outgoing sheep on its exact cell beneath the opaque stopped UFO until the existing lift hides it; never carry that actor with the departing body. Never reuse the outgoing DOM actor, restore a roster or replacement text card, show a sheep in flight, expose more than 8px of UFO body travel at either edge, retarget the camera on every bite or field replacement, or add another effect layer.
- Accumulate progress on the existing sheep art only: each recorded bite may grow its 80%-scale body and reveal restrained green wool marks up to 0.48 opacity. Return that same stored-energy idea only through progressively brighter existing pickup light, absorption in the fixed dock UFO core, and the existing final cell wave.
- The ending is one sequence: 0.28 seconds of empty pasture, convergence on centre cell `[26,3]`, one impact, then a 1.08-second Euclidean point wave rendered only through contribution cells. Use half-cell distance phases, a thin crest, a short wake, and distance attenuation; reveal each username cell when that same front reaches it. `SEONARU` remains the deterministic fixture; production uses `GITHUB_USERNAME`, repository owner, then Actions actor. Support A–Z, 0–9, and hyphen without silent truncation: 1–10 characters use 5×7, 11–13 use 3×5, and 14–26 use two centred 3×3 lines. Confirm, exit upward, then hold the uncontested word for 1.40 seconds. No drawn circle or ellipse, top rail, cell-by-cell plotter, laser, or independent ripple layer.
- Use the standard GitHub light and dark contribution palettes through CSS variables for the field, progress, and panel state; identity tags alone use deterministic varied hues. Keep `live.svg` as the auto fallback, derive forced `live-light.svg` and `live-dark.svg` from that exact animation, and select them in GitHub Markdown with theme-context URL fragments; do not add a theme dependency.
- Keep the command panel as one flat continuation of the pasture contribution grid: exact 12px inset/pitch, 10px rounded level-0 cells, a 53×5 surface, an 84px panel whose lower fence lands on the aligned 72px row, no nested cards or sci-fi frame, and borderless merges for `방목·양떼·잔디` plus one 202px live-camera surface.
- Treat `양떼` as waiting inventory, never a roster. Put the exact undispatched/total count before eight fixed brown pens with shared original sheep art and lower-right identity stamps; the eighth pen is also the unboxed UFO transfer bay. Preserve opening holes by boarding the initial field batch before departure: lower the UFO to the rightmost pen centre, absorb the fixed sheep under its body, then shift only visible in-grid sheep one adjacent pen right. Leave the bay empty as the last opening load lifts, wait until field deployment ends, shift the survivors once, and refill from the far-left pen toward the right until all eight pens are used. On later dispatch, repeat the same centre dock, lift the UFO clear before the queue moves, hold the empty pen, shift seven visible sheep right, and reveal the next hidden sheep only at far left. Keep the full outgoing sheep only on its collected field path and materialize the incoming field actor only at its first recorded work cell.
- The 190×40 clip must reuse the exact pasture and identical movement, pose, head, appetite growth, energy, and ear-tag tracks at 1.30x. Hold each route-diverse hero until collection, keep every hero framed with restrained chronological reframes, and snap rather than pan for reduced motion. Show `포만 n/capacity`, ten horizontal cells, and the original static UFO after completion.
- Keep the right-side 36×4 recorded-bite projection, persistent contribution-green marks, one latest-bite cursor/pulse, and two low-opacity 1.5px footprints only on cells already eaten by the current hero; hide pulse and footprints at completion. Never restore an uncapped roster, roster number, space-between layout, legend, decorative camera sheep, or identity-colored whole map cell. Preserve the brown perimeter and drive field, inventory, and hero visibility from visual spawn/hide times.
- Keep visual-fixture work local. Credentials, live GitHub calls, deployment, publication, and external writes require explicit approval.
- Use direct execution by default. Treat one-off product corrections as task work, not agent or harness evolution: do not create feedback/proposal/promotion records or bump an agent version unless the evidence exposes a repeated process defect or changes how future work is performed.
- Keep one product candidate open across iterative user corrections. Use the smallest deterministic regression check for ordinary fixes, and run one fresh independent visual Gate only when the complete candidate is ready for promotion, publication, or explicit final review.
- Keep only the current verification and one rollback point in host-loaded guidance. Do not append historical verification prose here; preserve older evidence in the digest-bound evolution archive and retrieve it only when a matching regression requires it.
- Record verification evidence once in the active checkpoint or receipt. User-facing documentation should describe behavior, not duplicate Gate transcripts.
- For identity or handoff feedback, trace the actor lifecycle and inspect adjacent 700px frames before choosing another coordinate metric; the Gate must judge at least 1.6 seconds after every reveal in continuous playback plus 30fps and 120fps coordinates, and reject a reused roster actor, any offscreen sheep transform, stale outgoing coordinate, non-adjacent visible jump, cross-field UFO body exposure, broken green blink, sheep visible in flight, movement before the departing body is hidden, missing destination flash, or spawn/ready transform outside the exact drop.
- Before ending a source-changing session, run the currently loaded NULNUL skill's documentation-debt check against this repository.
- Final verification: 2026-08-21, local v59 relaxes the exact-field camera from 1.45x to 1.30x, exposing 11.5% more source width. Reachable grass candidates are unique and prefer restrained three-to-four-edge real routes; full outgoing sheep follow their recorded cleared approach through the final pre-work cell before exact pickup, with no idle animation or artificial bridge. Comparable 30fps sampling raises average root-moving sheep from 1.84 to 2.43 and cuts frames with at least four fully still sheep from 164 to 27. Native 700px dark/light, 350px, reduced-motion, and adjacent pickup frames remain clear. Fresh 30fps/120fps field↔camera probes report zero jumps or mismatches, and full-duration inventory probes retain a maximum of eight actors with zero failures. `npm run visual:fixture`, `npm run check`, and `git diff --check` pass with a 2,141,551-byte, 63.736-second fixture. A fresh independent Gate scored 19/20 with no blocker; pacing remains 3/4 because of the total runtime. Keep this candidate local and unpushed.
- Rollback verification: local v58 keeps the accepted eight-pen inventory and uses the 1.45x camera without unique-route preference or the recorded pickup approach extension. Its `npm run visual:fixture`, `npm run check`, and `git diff --check` evidence passed at 2,118,037 bytes and 61.864 seconds; a fresh independent Gate scored it 19/20 with no blocker.
