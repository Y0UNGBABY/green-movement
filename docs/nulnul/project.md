# nulnul project setup

Status: initial, removable hypothesis.

## Goal

Turn the GitHub contribution graph into a polished animated miniature pasture that looks intentional and premium in a profile README, without crude or competing effects.

## Current milestone

Keep local v59 unpushed until the user inspects it. The centre status merge puts `양떼 current/total` before eight fixed pens and uses the rightmost pen itself as the unboxed UFO transfer bay. Before the first field entry, the dock UFO approaches and settles at that pen's exact centre, then absorbs each fixed sheep in its own hold. All eight coordinates remain fixed across the six loads, so accumulated vacancies never collapse the array or pull an old sheep back left. Those holes remain until the last deployment UFO is fully offstage; the two survivors then shift once and six pending actors reveal individually from the far-left pen toward the right, filling all eight pens without reading as sheep exiting the UFO. Every later turnover follows approach→settle→absorb→lift→seven-actor shift→delayed far-left refill→settle→exact drop. The exact-field camera is relaxed to 1.30x, exposing 11.5% more source width than 1.45x. Reachable grass candidates are unique and prefer restrained three-to-four-edge real routes; a full outgoing sheep follows its recorded cleared approach through the final pre-work cell instead of freezing at its last bite. On the field, UFO-body visibility remains snap-only 0/1 and the opaque stopped body paints above exact-cell sheep during both drop and pickup.

Accepted and published v33 removes the outgoing actor's movement and pose keyframes at the shared turnover boundary so the incoming sheep cannot jump back to the pickup coordinate after its exact-drop reveal. The deterministic check now rejects any non-adjacent visible sheep jump and any unplanned coordinate between replacement movement start and bridge completion.

Accepted v35 keeps the v33 simulation and the useful v34 typography cleanup, but replaces the rejected text-heavy handoff. Every roster identity owns one deterministic 3x3 ranch tag shared by its field actor, selected portrait, and roster slot. The next slot cues 0.54 source seconds before pickup; after the outgoing actor disappears, the selected region stays empty for 0.08 source seconds before the hungry identity appears. Galmuri7, `방목·양떼·잔디`, the prior brown perimeter, and the icon-free grid remain.

Accepted v36 applies the user's simpler panel direction without changing that handoff: the shared tag is one square cycling through the four GitHub contribution levels, the numbered roster is square and spans x=212–650 with `space-between`, compact roster meters are removed, and the selected sheep, tag, number, and sole ten-cell fullness meter share the left row. `방목·양떼·잔디` equally divide the entire panel width, while selected and header visibility switches use `step-end` so the left sheep never fades gradually.

Accepted v37 keeps that compact layout but moves the field mark from wool to the left ear, lets it follow the existing head nod, and places the selected portrait's mark at the same ear. Each numbered roster square now fills left to right from its existing recorded-bite progress clip, restoring visible per-sheep fullness without bringing back a second compact ten-cell meter.

Accepted v42 restores that v37 square-grid hierarchy after the rejected v38–v40 rectangular-meter detour. Every reachable identity now has a distinct deterministic HSL tag, but the hue is deliberately limited to the field and selected ear, a small swatch immediately left of the selected horizontal meter, and a small top-right roster marker. The roster surfaces and left-to-right fullness remain in the GitHub contribution palette; `방목·양떼·잔디` is unchanged.

Accepted v43 changes only selected fullness into the requested game-character gauge: five broad blocks inside one restrained rail, continuously clipped by recorded bites so the final block can fill partially. Exact `포만 n/20`, the adjacent identity swatch, square roster, header, and all motion remain unchanged.

Accepted v44 turns the panel into a continuation of the pasture instead of a separate dashboard. Its 53×5 background uses the same 12px inset and pitch, 10px rounded level-0 cells, and full opacity; borderless 18/17/18-column status and 8/9-column selected/fullness merges sit on that surface. Increasing the panel from 78px to 84px moves its lower fence from the half-tile 66px row to the aligned 72px row and removes the oversized gaps beside both bottom corners.

Accepted v45 removes the roster-card language entirely. Normal identities are the same borderless 10×10 rounded pasture cells aligned to the panel grid, with bite-driven horizontal fullness and a 2px identity corner. Only the focused identity reveals a centered number and external four-corner cursor; ready, active, and extracting states use short lines rather than boxes, and terminal shows neither number nor cursor.

Rejected v46 put one miniature sheep per owned identity into the panel and expanded selected fullness to twenty cells. The user found the irregular density, duplicated sheep, and long meter incompatible with a lower control surface.

Accepted v47 deletes the roster rather than restyling it. Exact selected fullness returns to ten separated cells, while the right side becomes a fixed 36×4 projection of real bite coordinates. Contribution-green marks accumulate, one corner cursor and pulse identify the latest bite, and the map remains stable across 10/28/62 identities without any per-sheep slot geometry.

Accepted v48 turns the left selected surface into a clipped tracking view of the actual contribution grid. It starts from each sheep's exact UFO drop, pans for at most 0.14 source seconds to selected bite targets, clears local grass at the recorded bite, and keeps the original tagged sheep centered. During handoff the content remains blank while the camera retargets. On the right map, only the current selected sheep's already-eaten cells expose two 1.5px low-opacity footprints matching its ear tag; terminal removes footprints and camera while retaining the green activity map.

Accepted v49 replaces the selected-sheep illustration and bite-by-bite retargeting with a true enlarged view of the running field. The 190×40 crop reuses the exact pasture scene and identical field-sheep movement, pose, head, growth, energy, and ear-tag tracks at 1.55x. A lifetime-plus-neighbour score chooses six representative heroes in the 28-member fixture; each remains until collection and the camera uses only 24 route reframes across 201 bites. The next hero appears with its HUD only after the 0.55-source-second pan completes. The terminal UFO and two completion lines are centred as one group within the left 202px camera region.

Accepted local v50 replaces roster-style turnover with a six-pen waiting inventory. The count represents only sheep that have not yet deployed, so it decreases from total to zero and collected sheep never rejoin or replay beside the dock. Opening deployment preserves its 4/6 holes before one in-grid group shift; later dispatches expose one empty front pen, move only the five visible occupants one adjacent slot, and reveal the next hidden sheep only in the last pen. Each replacement drops directly on its first recorded bite cell. Relative active-week appetite assigns deterministic high/normal/low capacities with an average target of 20; all sheep begin at scale 1, then bite-driven growth reaches a high/low full-size ratio of about 1.20.

Accepted local v51 mirrors that waiting inventory around the right dock and uses the same direction for opening and later dispatches: rightmost load, visible vacancy, one rightward group shift, leftmost reveal, settle, then exact field drop. Turnover suppresses competing crumbs and map pulses, and the existing fixed UFO core briefly absorbs pickup brightness before returning clean for loading. The camera rotates deterministically through context, route, and grazing heroes with fewer chronological reframes and no visible queue overlap; its 0.55-source-second handoff stays blank. A shared inventory symbol removes duplicated art, while the existing growth, tag, and CSS paths supply the late-readable 0.72 progress curve, theme outline, and 350px correction.

Accepted local v52 makes that rightmost load physical and legible using only existing actors. One existing pen rail opens and recloses; the sheep holds, moves horizontally, and is progressively clipped behind the fixed UFO while its identity stamp remains last. The existing core supplies the single confirmation beat, followed by a closed empty-pen hold and the unchanged adjacent queue shift. Reduced motion snaps the load and shift without restoring a fade or exposing a sheep in flight.

Rollback local v53 right-aligns partial six-pen inventories and keeps every queue shift on matching coordinates, but it still has no opening inventory-to-UFO boarding and paints field sheep above the stopped pickup UFO.

Current local v59 keeps the v58 eight-pen centre-dock grammar and widens the 190×40 exact-field camera from 1.45x to 1.30x without changing its hero lifecycle or actor tracks. The simulation removes duplicate reachable grass destinations, prefers real routes three to four grid edges long when available, and records the cleared approach already needed for the next worker. At turnover, a full outgoing sheep follows that existing route through its final pre-work frame before exact pickup. This reduces inert flock clusters without adding idle animation, artificial bridge cells, or a second movement system.

Observable completion check: `npm run check`

Final verification: 2026-08-21, local v59 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 2,141,551-byte, 63.736-second fixture. The 1.30x camera exposes 11.5% more source width than v58 while retaining exact pasture and actor tracks. Unique reachable destinations and restrained three-to-four-edge route preference raise comparable 30fps average root-moving sheep from 1.84 to 2.43; frames with at least four fully still sheep fall from 164 to 27. Three adjacent pickup sequences show full sheep completing their recorded cleared approach, settling beneath the UFO, and disappearing through pickup with no fake idle motion. Native dark/light, 350px, and reduced-motion frames remain clear; fresh 30fps/120fps field↔camera probes report zero jumps or mismatches, and inventory probes retain a maximum of eight actors with zero failures. A fresh independent Gate scored 19/20 with no blocker; only total runtime remains 3/4. It remains local and unpushed.

Rollback verification: 2026-08-21, local v58 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` at 2,118,037 bytes and 61.864 seconds. It keeps the accepted eight-pen inventory and 1.45x camera without v59's unique-route preference or recorded pickup approach extension. A fresh independent Gate scored it 19/20 with no blocker.

Final verification: 2026-08-19, v49 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,975,600-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Native 700×212 dark/light representative plus 10/62 frames show the exact field enlarged at 1.55x inside a 190×40 crop, including identical sheep position, pose, head, growth, energy, and ear tag. The representative camera chooses heroes `0,4,7,13,15,22`, holds each until collection, and uses 24 route reframes rather than 201 bite cuts. At the first two handoffs, HUD and footprints remain blank through the 0.55-source-second pan and appear with the framed hero on the exact arrival frame. Reduced motion, ten-cell fullness, fixed bite map, terminal cleanup, the completion group centred at x=113 within the left 202px region, selected opacity, and fresh 30fps/120fps field continuity remain green.

Final verification: 2026-08-19, v48 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,991,724-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Native 700×212 and 3× dark/light 10/28/62 frames show an 82×34 actual-grid tracking crop with the original 30×25 tagged sheep centred, ten separated fullness cells, a fixed 36×4 contribution-green bite map, one latest-bite cursor, and two restrained identity footprints only on the current sheep's past bites. Handoff adjacent frames remain clean; reduced motion uses step-end camera movement and hides pulses; terminal keeps map history while hiding camera, selected content, pulse, and footprints. Selected opacity has zero failures across 1,970 samples; fresh representative incoming22 and 10-member incoming6 probes at 30fps/120fps report zero continuity failures.

Final verification: 2026-08-19, v45 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,809,731-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Native 700×212 and 3× dark/light 10/28/62 frames show borderless aligned pasture-cell rosters, one focused number/cursor, 2px identity markers, horizontal partial fullness, and compact state lines without overlap or clipping. A 60fps probe found maximum one number and cursor with zero failures across 2,692 samples; terminal has neither. The v44 panel/fence, v43 health states, handoff, reduced motion, and finale remain intact. Selected opacity has zero failures across 1,970 samples; 30fps/120fps probes found zero continuity failures for all representative and 10-roster incoming actors.

Final verification: 2026-08-19, v44 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,807,628-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Native 700×212 dark/light frames show the pasture and panel sharing the exact cell pitch, palette, rounded geometry, and inset; both lower corners retain the regular 12px fence rhythm. Borderless merged status, selected, and fullness regions remain readable without restoring nested cards. The v43 five-block 0/4/18/20 health states, x=216–646 square 10/28/62 rosters, handoff, reduced motion, and terminal state all pass without clipping. Selected opacity has zero intermediate values across 1,970 samples; 30fps/120fps probes found zero outside reveals, non-adjacent jumps, velocity spikes, or early movement for all 22 representative and six 10-roster incoming actors.

Final verification: 2026-08-18, v42 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,803,485-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Native 700px dark/light 10/28/62 and 3x frames show the unchanged full-width status row, clean square two-row roster spanning x=212–650, original selected `포만 n/20` plus ten horizontal cells, green roster-square fill moving left to right, and matching distinct color only at the ear, selected-meter swatch, and each square's top-right corner. Numbers remain readable without marker collision. Reduced motion and terminal frames pass; selected opacity has zero intermediate values across 1,970 samples; 30fps/120fps probes found zero outside reveals, non-adjacent jumps, velocity spikes, or early movement for all 22 representative and six 10-roster incoming actors.

Final verification: 2026-08-18, v37 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,790,045-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Fresh 3x and 700px forced dark/light plus reduced-motion frames show the tag attached to the left ear rather than the body and the numbered 10/28/62 roster squares filling without hiding their numbers or identity colors. The full-width header, selected ten-cell meter, v35 cue and blank handoff, exact-drop actors, and terminal state remain intact. A browser probe found zero intermediate selected opacity values across 1,970 samples, and 30fps/120fps probes found zero outside reveals, non-adjacent jumps, velocity spikes, or early movement for all 22 representative and six 10-roster incoming actors.

Final verification: 2026-08-18, v36 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,780,059-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Forced dark/light and reduced-motion 700px frames show four contribution-color 1x1 tags, a full-width equal status row, selected sheep/tag/number/fullness spacing, and square 10/28/62 two-row rosters without compact meters or clipping. The v35 next-slot cue, exact 0.104-second blank handoff, incoming selection, exact-drop actor, and terminal state remain intact. A browser probe found zero intermediate selected opacity values across 1,970 boundary samples, and fresh 30fps/120fps probes report zero stale coordinates, outside reveals, non-adjacent jumps, velocity spikes, or early movement for all 22 representative and six 10-roster incoming actors.

Final verification: 2026-08-18, v35 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,851,823-byte, 44.863-second fixture. A fresh independent Gate scored it 20/20 with no blocker. Forced dark/light 700px frames show the next slot cue 0.702 playback seconds before pickup, outgoing sheep 01 and its full tag at 20/20, an exact 0.104-second empty selected region with FIELD 5/6, incoming sheep 07 and its matching empty tag at 0/20 before the remote drop, and the same tag on the exact-drop field actor. The first 300 tags are unique within a 492-tag capacity; all 28 representative field/selected/roster mappings match. Fresh 30fps/120fps probes over all 22 representative and six 10-roster incoming actors report zero stale coordinates, outside reveals, non-adjacent jumps, velocity spikes, or movement before UFO hide. All 201 arrivals and pulses, 50 stops, exact bridge timing, early pickup, 10/28/62 layouts, reduced motion, terminal state, and 53 finale phases remain green. Feature `6886ea8` and successful workflow `32107648450` published project assets in `0496aa0`, profile assets in `7f59feb`, immutable profile embeds in `cd2fd6b`, and profile prose in `6115be4`; all public project/profile theme hashes match byte-for-byte.

Final verification: 2026-08-18, v33 passes `npm run visual:fixture`, `npm run check`, and `git diff --check` with a 1,336,351-byte, 44.863-second fixture. The pre-fix negative control failed the new bridge-continuity assertion, and a fresh independent Gate scored v33 20/20 with no blocker. Fresh 700px SHEEP 06/07 frames show exact-drop holds followed only by the first adjacent bridge step. Representative incoming 22 and 10-roster incoming 6 browser probes report zero stale coordinates, outside reveals, non-adjacent jumps, or velocity spikes over 1.6 seconds at both 30fps and 120fps. All accepted causal, bridge, roster, theme, reduced-motion, pickup, finale, and terminal checks remain green. Feature `8059a81` and workflow `32097810199` published project assets in `48d1b46`, profile assets in `6d707c5`, and the immutable profile embed in `3c4212e`; all public project/profile theme hashes match byte-for-byte.

Final verification: 2026-08-18, v32 passes `npm run check`, `npm run visual:fixture`, and `git diff --check` with a 1,336,713-byte, 44.863-second deterministic fixture. A fresh independent Gate scored it 20/20 with no blocker. Independent browser probes confirm 28 unique roster actors, zero offscreen sheep transforms, and zero outside or unstable first-reveal failures at both 30fps and 120fps. Fresh 700px dark/light lifecycle frames show one fixed 0.182-second UFO rhythm across entry, deployment, turnover, collection, finale convergence, and exit. All 201 causal arrivals, 22 replacements, 50 stops, exact bridge timing, early pickup, terminal panel UFO, and 53 finale phases remain green. Feature `09eb79b` and workflow `32094399926` published project assets in `b90212a`, profile assets in `58afa87`, and the profile embed in `811948f`; project/profile live, light, and dark SVG hashes match byte-for-byte and the profile README points to the immutable asset commit.

Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,273,474-byte, 44.122-second deterministic fixture. A fresh independent Gate scored v31 20/20 with no blocker. Forced dark/light, reduced-motion, and 10/28/62 adjacent frames show a short visible first entry, green body-hidden middle blink, visible exact-drop approach, and the same path grammar across deployments, collection, finale convergence, and exit. All 22 replacements preserve the 0.104-second exchange, preflight `INBOUND · 0/20`, exact-drop clean sheep reveal, and cleared bridge. Initial sheep remain fixed at their drop until UFO departure. All 201 arrivals and pulses, exact 0.312-second bridge edges, serialized stops, endpoint separation, early pickup, terminal panel UFO, and 53 finale phases remain green.

The following final verification is the published v30 rollback point.

Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` pass with a 1,229,601-byte, 48.581-second deterministic fixture. Forced dark 700x206 adjacent frames show the first UFO absent until its exact-drop flash, opening deployments 2–6 absent through each 0.052-second jump, and sheep 3 and 4 revealed only at their destinations. All 22 replacements use the same hidden 0.104-second jump, pre-jump `INBOUND · 0/20`, destination light, clean white exact-drop reveal, stopped hold, and cleared adjacent bridge. All 201 arrivals and pulses, bridge timing, serialized stops, endpoint separation, early final pickup, roster layouts, themes, reduced motion, terminal state, and 53 finale phases remain green. A fresh independent Gate scored v30 20/20 with no blocker. Feature `8ea8215` and workflow `32086601719` published project asset `6f8af55`, profile asset `f4e30db`, profile embed `0094973`, and profile prose `32ef11e`; project/profile theme assets are byte-identical and the public 10-member dark SVG is 583,478 bytes over 35.204 seconds with six handoffs.

The following final verification is the previous published v29 rollback point.

Final verification: 2026-08-18, `npm run check` and `npm run visual:fixture` passed with a 1,230,931-byte, 48.581-second deterministic fixture. Forced light/dark 700x206 frames show exact-drop opening materialization and 22 visible 0.104-second UFO speed exchanges with existing streak and arrival light. Every panel handoff identifies a new `INBOUND · 0/20` sheep before flight; the clean white replacement appears only at its remote drop, settles under the stopped UFO, then follows cleared adjacent bridge cells. All 201 arrivals and pulses, exact 0.312-second bridge edges, serialized stops, footprint separation, early final pickup, 10/28/62 rosters, reduced motion, terminal state, and 53 finale phases remain green. A fresh independent Gate scored v29 20/20 with no blocker. Feature `977b065` and workflow `32085064689` published project asset `ccfc73f`, profile asset `cae1d93`, profile embed `c322968`, and profile prose `12241ec`; project/profile theme assets are byte-identical and the public 10-member dark SVG is 599,943 bytes over 39.884 seconds with six handoffs.

## Constraints and permissions

- Preserve the existing TypeScript/ESM SVG pipeline and validate at the real 700px README presentation size.
- Prefer one coherent motion system over adding more independent effects.
- Existing user changes remain authoritative; harness setup must not overwrite generated output or credentials.
- External services, credentials, deployment, public writes, and global configuration require explicit user approval.

## Inspected roster

- Host surface: Codex with canonical `AGENTS.md`; existing `CLAUDE.md` remains compatible and points to it.
- Skills: session catalog inspected. Relevant installed skills include NULNUL, Ponytail, Firecrawl for a named research gap, image generation, code review, GitHub, Figma, Drive, Notion, and context-compression utilities. The user explicitly retired `frontend-design` for this project's motion work because the project-specific study now covers that job.
- Plugins: bounded `codex plugin list --json` was attempted; status is unknown because a stale `nulnul-harness` marketplace snapshot has an unsupported manifest. No global repair was attempted.
- Agents: no project-local skills or agent definitions exist under `.agents`; Navigator, Worker, and Coach remain one logical execution role until independent evaluation is needed.

## Capability requirements

| Required job | Input → output | Quality check | Data or permission boundary |
| --- | --- | --- | --- |
| Motion and art direction | Current SVG layers and motion → one coherent visual direction | Frozen benchmark reaches at least 16/20, no dimension below 3 | Local repository reads and writes |
| Minimal implementation | Approved direction → smallest source diff | `npm run check`; no speculative dependency | Local repository writes |
| Deterministic regression | Synthetic grid → representative SVG | No missing layers, `NaN`, or `undefined` | Local generated file only |
| Session continuity | Verified result and next action → resumable state | Evolution validator passes | Project-local state; one Navigator writer |
| Independent promotion | Candidate plus baseline → promote, reject, or rollback | Fresh Gate reproduces benchmark and mechanical checks | No authority or permission expansion |

## Candidate evidence

| Candidate | Source | Fit and quality evidence | Permission and license | Verification status | Decision |
| --- | --- | --- | --- | --- | --- |
| Green-movement motion study | User-designated Obsidian project research | Sourced staging, continuity, 5x7 typography, numeric shot timing, rejection rules, and Gate questions specific to this SVG | Local research; no product dependency | Used to preregister the crop-signature shot | Reuse as motion authority |
| `frontend-design` | Installed Codex skill | Broad visual guidance now overlaps the project-specific motion study | Local guidance, Apache-2.0 | Repeated activation produced no uncovered project job | Do not activate for this project |
| Ponytail 4.9.0 | Installed plugin by Dietrich Gebert | Enforces native/stdlib reuse and minimum correct diff | Local guidance/hooks, MIT | Manifest and instructions inspected | Reuse for context economy and scope control |
| NULNUL harness 2.1.0 | Installed plugin by SeoNaRu | Supplies setup contract, continuity, validators, and evidence-gated evolution | Local project files, MIT | Current cache instructions and evolution workflow inspected | Reuse for harness ownership |
| Native Node/TypeScript | Existing project stack | Already builds SVG without runtime dependencies | Local execution, existing project license boundary | `npm run build` and strict TypeScript check pass | Reuse for implementation and checks |
| Image generation | Installed system capability | Produces raster assets, while this product's source of truth is code-generated SVG motion | Would add an asset workflow and review burden | Adequate but mismatched | Skip until a raster asset is explicitly needed |
| New project-local skill or agent team | Repository-local addition | No recurring uncovered job remains after installed skills and direct execution | Would add maintenance and coordination | Rejected for day one | Do not create |

Create a custom capability only when the checked candidates are inadequate.

## Capability routing

| Capability | Source | Job | Activate when | Check | Permission boundary | Remove or replace when |
| --- | --- | --- | --- | --- | --- | --- |
| Green-movement motion contract | Project research plus `AGENTS.md` | Direct and critique pasture motion | Every motion change | Fixed 700px frames plus frozen visual benchmark | Repository reads/writes; approved Obsidian research writes | Revisit research only when a named design gap is uncovered |
| Ponytail | Installed plugin 4.9.0 | Prevent effect accumulation and speculative abstraction | Any implementation or refactor | Small diff plus `npm run check` | No permission expansion | User disables it or it blocks an explicit requirement |
| NULNUL harness | Installed plugin 2.1.0 | Own setup, continuity, feedback, and promotion rules | Setup, resume, or measured correction | Project/evolution validators and product check | Project-local state only | Continuity and evolution are no longer needed |
| `scripts/check-svg.mjs` | Repository-local native Node script | Build one deterministic representative SVG | Every source check; `--write` for visual inspection | `npm run check` | Writes only `dist/visual-fixture.svg` with `--write` | A stronger existing test replaces it |
| Documentation debt detector | Script shipped with active NULNUL skill | Warn when source outpaces durable guidance | End of a source-changing session | Detector exits zero | Read-only repository check | Source and durable guidance stop evolving together |

Available capabilities and capabilities active for the current task are separate sets. Active now: the project motion contract, NULNUL, Ponytail, native Node/TypeScript, and the deterministic SVG check. `frontend-design` is explicitly inactive.

## Setup decisions

- Reuse now: the preserved project motion contract for direction, installed NULNUL for continuity, Ponytail for context economy, and native Node/TypeScript for checks.
- Add now: canonical `AGENTS.md`, project/evolution contracts, one frozen visual benchmark, and one dependency-free SVG fixture check.
- Needs approval: live GitHub credentials, downloads, plugin repair or registration, deployment, publication, and external writes.
- Skip: raster generation, a custom skill, multiple persistent agents, dashboard, counted-deliverable function, and single-writer lock; they have no demonstrated current job. Independent Gate separation is deferred until a promotion candidate exists.

## Agent topology

Single-agent direct execution. The Navigator owns outcome, permissions, state, and synthesis; the Worker performs the bounded motion or code change; the Coach turns reproduced feedback into one proposal. These logical roles share one execution context. A fresh Gate that did not author the candidate must independently score the frozen fixture and run mechanical checks before promotion. Handoff is candidate diff plus benchmark scores to the Navigator. Remove the separate Gate step when no evolution candidate is being promoted.

For live state and version ownership, use `docs/nulnul/evolution.json`.

## Evolution baseline

- Representative run: deterministic 53×7 synthetic contribution grid rendered at 700px through `npm run visual:fixture`.
- Primary metric: the single score defined in `docs/nulnul/visual-benchmark.md`; baseline 8/20, target at least 16/20 with no dimension below 3.
- Guardrails: `npm run check`, no network or credentials, no invalid SVG values, no unapproved writes, and no SVG-size increase above 15% without measured visual benefit.
- Rollback: keep the current source state as champion; reject or revert only the candidate motion diff when the Gate score or a guardrail fails.

## Assumptions and accepted evolution

- Assumption: one coherent miniature-pasture motion pass can close the visible quality gap without replacing the rendering architecture.
- Revisit when: the frozen benchmark cannot reach target without a different character or scene system.
- Accepted change: initial harness only; both NULNUL validators and `npm run check` pass, the frozen fixture is 4,099,243 bytes, baseline product behavior is unchanged, and every harness file is removable.
- Rejected candidate: the eight-sheep version with a simplified UFO, continuous hover, and 3D banking passed mechanical checks but failed user review as visually cheaper and too slow.
- Rejected candidate: the restored UFO stayed at a fixed zero-degree heading and looked downward during travel; missing pickup departure keyframes also caused a long unintended drift.
- Rejected candidate: directional-flight v4 fixed heading and pickup drift but remained too slow and lacked a researched scene grammar.
- Rejected candidate: cinematic-flight v6 improved the opening but left the UFO occupying the pasture, retained awkward stationary sheep pulses and long-arc turns, and rendered flowers as dots.
- Rejected candidate: pasture-performance v7 fixed scene ownership, turns, stationary pulsing, and flower silhouette but left grass up to 1.05s ahead of visible sheep, kept sheep too large, and lacked a decisive grazing-start beat.
- Superseded candidate: bite-timing v8 established synchronized grass reactions, 80% sheep scale, and the brace-impact bite beat, but the six sheep still entered the middle act as independent nearest-target agents with no staged event.
- Rejected candidate: flock-relay v9 added three readable pair launches but left level-dependent two-to-five-tick grazing stops, a 76.00-second runtime, and no memorable ending payoff.
- Rejected candidate: crop-signature v10 kept relay delays, flowers, a fixed flock rule, and a line-like laser; user review requested immediate movement, no flowers, density-scaled sheep, a stronger beam, theme matching, and a pre-drop scan.
- Rejected candidate: scan-and-deploy v11 used a spotlight-like sweep whose deployment decision was not readable, then moved the UFO along a top rail to plot 123 cells one by one. The user rejected both the scan and writing as effect-led and mechanically staged.
- Rejected candidate: cinematic-field v12 replaced the plotter ending successfully but its full-width scan line still read as a cheap laser/barcode reader, crosshair locks looked like an unrelated game HUD, and paired drop locations clustered vertically.
- Rejected candidate: silent-observation v13 removed the HUD but retained a visible centre hold, a three-pixel pre-arrival correction, a route with repeated reversals, and an elliptical finale that still read as a growing circle.
- Rejected candidate: continuous-flight/grid-wave v14 fixed route continuity and the finale but still treated field centre as a mandatory opening waypoint before visiting the first drop.
- Rejected candidate: direct-drop v15 fixed the opening but still derived flock size from grass density and inferred bite acting from any stationary run, so blocked waits could look like eating.
- Superseded candidates: causal-bite/fixed-flock v16, Euclidean cell-wave v17, and middle-act escalation v18 were never independently promoted as standalone candidates; their surviving causal bite, finale, stored-energy, and long-username behavior is covered by the accepted v19 Gate.
- Accepted candidate: fullness-turnover panel v19 replaces the fixed cast with contribution-energy staffing at 1/2/4/6 simultaneous sheep. Each route fills a 20-energy sheep, triggers an immediate serialized UFO exchange, and continues with a hungry roster sheep; the 700px panel shows the selected sheep plus every roster member in two rows without a 12-sheep cap. Deterministic 100- and 300-cell probes expose 22 and 62 roster sheep respectively. The representative 966,894-byte fixture lasts 36.44 seconds, exposes 28 sheep and 22 exchanges, clears all 201 cells, and adds no dependency or effect layer. A fresh independent Gate scored it 19/20 and accepted it with no blocker.
- Accepted candidate: command-panel readability v20 keeps the original sheep only in the selected card at 28×23, replaces repeated miniature sheep with row-major numbered selection tiles and individual green bars, highlights the current tile, and uses density-aware roster spacing. The 28-member grid reads as 01–14/15–28 and the dense 62-member probe as 01–31/32–62 in both themes; 1-, 2-, and 9-member layouts remain deliberately grouped. The 981,652-byte representative fixture changes no motion or simulation, and a fresh Gate accepted it at 19/20 with no blocker.
- Accepted candidate: turnover-identity/early-pickup v21 hides the outgoing sheep for 80ms before the next roster ID descends, exposes the brief service dip through FIELD, and routes the single UFO to the earliest physically reachable finished final sheep instead of waiting for all six. The 991,057-byte fixture begins final pickup at 27.10 seconds with grass at 90%, finishes in 35.68 seconds, and a fresh Gate accepted it at 19/20 with no blocker.
- Accepted candidate: profile-theme/panel-sections v22 derives explicit light and dark 700x206 SVGs from the same animation, switches them with GitHub theme-context image URLs, immediately refreshes the profile on relevant main pushes, gives the light theme a true light HUD, and frames the selected unit and full roster as separate bodies. The 991,941-byte fixture changes no motion, and a fresh Gate accepted it at 19/20 with no blocker.
- Accepted candidate: GitHub pasture panel v23 removes the nested command-card frames, reuses the contribution canvas, border, cell, and green levels across one flat surface, distributes the 10-member probe as 5×2 and the representative 28-member roster as 14×2, and exposes active, extracting, and collected states. FIELD now decrements only when the selected sheep visually disappears and selection immediately moves to a visible sheep or PASTURE CLEAR. The 1,032,013-byte fixture remains 35.68 seconds with no motion change, and a fresh Gate accepted it at 19/20 with no blocker.
- Accepted candidate: complete-panel UFO v24 replaces the generic completion square with the exact original UFO as a static 30px state icon beside PASTURE CLEAR. Exact 10- and 28-member completion frames remain balanced in both themes; the icon is weaker than the active field UFO and username finale, adds no animation or asset, and leaves the 35.68-second timeline unchanged. The 1,034,299-byte fixture passed a fresh 19/20 Gate with no blocker.
- Accepted candidate: remote-turnover/pixel-panel v25 resolves each sheep's actual pickup position, separates every replacement drop by 3–7 cells, and lets the incoming sheep rejoin through cleared adjacent cells only. Fullness uses ten fixed contribution cells that jump from level-weighted bites instead of drifting with time. One shared 1.3× duration slows sheep, grass, UFO, panel, crumbs, and finale without reducing motion. The panel uses stepped corners, a two-row cell grid, monospace data, and the existing brown fence color only on its perimeter and major dividers. The 1,117,182-byte, 48.087-second fixture keeps all 201 arrivals, 22 turnovers, 50 UFO stops, terminal state, themes, and 53 finale phases; a fresh Gate accepted it at 20/20 with no blocker.
- Accepted candidate: stronger-energy/turnover-position v26 shows exact selected `ENERGY n/20`, flashes only the existing clipped meter at each of 201 recorded bites, and hides that pulse for reduced motion. Remote drops now clear the rendered UFO footprint plus one grass cell instead of using Manhattan distance; every replacement lands under the stopped UFO, crosses cleared adjacent cells at exactly 0.312 playback seconds per edge, and ends in a neutral pose when it arrives before the next bite. The 1,223,216-byte, 49.868-second fixture preserves 22 turnovers, 50 stops, 10/28/62 layouts, early pickup, terminal state, and 53 finale phases; a fresh Gate accepted it at 20/20 with no blocker.
- Accepted candidate: distance-paced/service-cut v27 removes the 0.40-second cap from long opening legs, ends each full-sheep collection with a short vertical UFO lift and hidden service interval, re-enters above the remote drop, and lands the hungry identity in a neutral front-facing stance before its exact-speed bridge. The 1,231,302-byte fixture lasts 49.686 seconds, retains 22 exchanges and all accepted v26 checks, adds no asset, dependency, or effect layer, and passed a fresh 20/20 Gate. The same Gate accepted lifecycle-plus-adjacent-frame diagnosis before coordinate metrics as the project-scoped Coach v2 rule.
- Accepted candidate: continuous-inbound handoff v28 removes the user-rejected opacity service cut. The panel switches to the next `INBOUND · 0/20` roster identity as collection finishes, the original UFO remains visible along one continuous pickup-to-drop flight, and the hungry sheep completes a close-to-ground landing before a short shared stop and departure. Exact checks reject any UFO opacity cut or intermediate teleport keyframe while preserving the 49.686-second runtime and all accepted causal, bridge, roster, theme, and finale behavior. A fresh independent Gate scored it 20/20 with no blocker.
- Accepted candidate: exact-spawn/speed-exchange v29 removes the opening 10px sheep descent and anchors every spawn, landing, and ready transform to its exact drop cell. A full outgoing sheep lifts with stored green energy; the panel switches to the new `INBOUND · 0/20` identity; the same visible UFO crosses the field in exactly 0.104 playback seconds with the existing streak and arrival light; then a clean white compressed replacement materializes only under the stopped UFO. The 1,230,931-byte fixture lasts 48.581 seconds and a fresh independent Gate scored it 20/20 with no blocker.
- Accepted candidate: unified flash-jump v30 removes the first offstage UFO descent and all visible deployment/turnover travel. The UFO stays hidden at the exact first drop until its flash, holds at each completed drop until a hidden 0.052-second jump, and uses the same hidden 0.104-second jump plus destination light for every replacement. Sheep remain absent until exact-drop reveal. The 1,229,601-byte fixture lasts 48.581 seconds and a fresh independent Gate scored it 20/20 with no blocker.
- Accepted candidate: visible-edge blink flight v31 replaces mixed invisible-entry, hidden-jump, visible-collection, visible-finale-convergence, and plain-exit travel with one shared path: at most 12px visible at each edge, the existing green light carrying the body-hidden middle, then an exact stop. Non-turnover service legs consume 0.12 source seconds / 0.156 playback seconds instead of distance-proportional wait; turnover remains 0.08 source seconds / 0.104 playback seconds, and the fixed finale timing is preserved. Initial sheep hold their exact drop until UFO departure, removing the apparent slide from under the carrier. The 1,273,474-byte fixture lasts 44.122 seconds and a fresh independent Gate scored it 20/20 with no blocker.
- Accepted candidate: independent-roster/fixed-rhythm v32 fixes the shared-slot actor root cause instead of masking the first visible frame. Every roster identity owns a distinct field SVG lifecycle, no actor is parked at `-9999px`, every incoming sheep materializes at its exact drop and waits until the departing UFO body is hidden, and every UFO leg uses the same 0.14-source-second / 0.182-playback-second blink rhythm with at most 8px visible at either edge. Independent 30fps and 120fps browser probes report zero unstable first reveals across all 28 actors, and a fresh Gate accepted it at 20/20 with no blocker.
- Accepted candidate: turnover-boundary continuity v33 changes the shared movement and pose skip from a strict interior boundary to include the outgoing history frame, preventing its pickup coordinate from reappearing after the incoming sheep's exact-drop reveal. Deterministic checks reject non-adjacent visible jumps and unplanned pre-bridge coordinates. Fresh 30fps and 120fps probes over 1.6 seconds report zero failures for all 22 representative and six 10-roster incoming actors; the 1,336,351-byte fixture remains 44.863 seconds and a fresh Gate accepted it at 20/20 with no blocker.
- Rejected candidate: Korean pixel handoff v34 established pinned Galmuri7, Korean status labels, the restored perimeter, and removal of fence-rail, energy-cargo, and check-badge clutter, but its simultaneous `수거/출동` cards asked too much reading of a fast exchange and were superseded before promotion.
- Accepted candidate: ranch-tag relay v35 preserves the v34 cleanup while replacing its text cards with a deterministic 3x3 identity tag, a 0.54-source-second next-slot cue, and an exact 0.08-source-second empty selected-region handoff. The same tag persists across field actor, selected portrait, and roster cell; 300 sampled identities are unique within a 492-tag capacity. The 1,851,823-byte fixture remains 44.863 seconds, below the frozen 4,099,243-byte baseline. Fresh independent dark/light 10/28/62 frames and 30fps/120fps continuity probes passed with a 20/20 Gate and no blocker.
- Accepted candidate: simple color-tag panel v36 replaces the visually dense 3x3 mark with one shared 1x1 contribution-color square, keeps the number as identity, removes compact roster fullness duplication, spreads the top status cells and lower content across their full widths, and snaps selected visibility. The 1,780,059-byte fixture remains 44.863 seconds and preserves the v35 cue, blank handoff, exact-drop actor, continuity, themes, reduced motion, 10/28/62 layouts, and terminal state. A fresh independent Gate scored it 20/20 with no blocker.
- Accepted candidate: ear-tag/roster-fullness v37 nests the field 1x1 tag beside the left ear under the existing head keyframes, aligns the selected tag to that ear, and reuses each identity's recorded-bite clip to fill its numbered roster square. The 1,790,045-byte fixture remains 44.863 seconds; fresh dark/light, reduced-motion, 10/28/62, opacity, and 30fps/120fps evidence passed a 20/20 independent Gate with no blocker.
- Rejected candidates: horizontal roster fullness v38 and dense variants v39–v40 replaced the clean square roster with per-actor ten-cell strips that became unreadable at 62 sheep; the user clarified that the original horizontal cells belonged to the selected fullness display and that varied identity color should remain a small marker.
- Accepted candidate: clean square roster/corner tags v42 restores the v37 square roster, keeps the selected horizontal ten-cell meter and every left-to-right bite fill, gives each reachable sheep a distinct HSL tag, and limits that hue to the ear, selected-meter swatch, and top-right roster corner. The 1,803,485-byte fixture remains 44.863 seconds; fresh native and 3x dark/light 10/28/62, reduced-motion, opacity, and 30fps/120fps evidence passed a 20/20 independent Gate with no blocker.
- Accepted candidate: selected health gauge v43 replaces only the selected meter with five broad partially fillable blocks while preserving exact text, identity, square roster, and motion. The 1,807,718-byte fixture remains 44.863 seconds and passed a 20/20 independent Gate.
- Accepted candidate: unified panel grid v44 extends the pasture's exact 53×5 contribution surface into the panel, aligns the lower fence to the 72px row, and replaces opaque cards with borderless grid-cell merges. The 1,807,628-byte fixture remains 44.863 seconds and passed a 20/20 independent Gate.
- Accepted candidate: cardless focus roster v45 removes persistent labels and full-box outlines from roster cells, leaving one focused number/cursor and compact state lines. The 1,809,731-byte fixture remains 44.863 seconds and passed a 20/20 independent Gate.
- Rejected candidate: miniature flock panel v46 put one sheep per identity into irregular panel spans and expanded selected fullness to twenty cells; the user rejected the density and duplicate actors as the wrong control-surface language.
- Accepted candidate: bite activity map v47 deletes roster geometry, restores ten separated fullness cells, and projects recorded bites into one stable 36×4 contribution-green map with a latest-bite cursor and pulse.
- Accepted candidate: tracking pasture map v48 reuses the actual contribution grid in an 82×34 selected camera, centres the original 30×25 tagged sheep, pans only to selected spawn/bite targets, and limits identity attribution to two low-opacity dots on the current sheep's past map cells. The 1,991,724-byte fixture remains 44.863 seconds; native and 3× dark/light 10/28/62, reduced-motion, handoff, terminal, opacity, and 30fps/120fps evidence passed a 20/20 independent Gate with no blocker.
- Accepted candidate: live hero pasture camera v49 replaces the decorative selected view with a 190×40, 1.55x crop of the exact running field and identical sheep actors. Six long-lived representative heroes are held through collection and followed with 24 actual-route reframes; a 0.55-source-second blank-HUD pan joins subjects, and the final UFO/text group is centred at x=113 inside the left 202px region on one grid-aligned level-0 merge. The 1,975,600-byte fixture remains 44.863 seconds and passed a 20/20 independent Gate with no blocker.
- Flower rendering source was deleted; only a negative deterministic assertion remains to prevent accidental reintroduction.

## Continuity

- Active checkpoint: `docs/nulnul/evolution.json`
- Resume rule: validate the active checkpoint, confirm repository reality, then perform exactly the recorded next action.
- Personal promotion scope: project-local unless a private personal evolution home is explicitly configured.
